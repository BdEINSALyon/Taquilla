<div class="container margin-top">
  <h1>Vos billets pour <%= @event.name %></h1>
  <p>Pour compléter l'achat nous avons besoin d'informations supplémentaires pour les billets commandées.</p>
  <% @cart.tickets.each_with_index do |ticket, index| %>
    <div class="panel panel-default">
      <div class="panel-heading">
        <h4>Billet <%= index + 1 %> - <%= ticket.pass.name %></h4>
      </div>
      <div class="panel-body">
        <h5>Informations du porteur</h5>
        <div class="form-group">
          <input type="text" name="tickets[<%= ticket.to_sgid %>][firstname]" class="form-control" title="Prénom" placeholder="Prénom">
        </div>
        <div class="form-group">
          <input type="text" name="tickets[<%= ticket.to_sgid %>][lastname]" class="form-control" title="Nom" placeholder="Nom">
        </div>
      </div>
    </div>
  <% end %>
</div>