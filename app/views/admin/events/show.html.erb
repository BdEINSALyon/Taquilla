<h1><%= back_link admin_events_path %> <%= @event.name %></h1>
<div class="row">
  <div class="col-xs-6">
    <%= link_to 'Editer', edit_admin_event_path(@event), class: 'btn btn-block btn-primary' %>
  </div>
  <div class="col-xs-6">
    <%= link_to 'Editer les billets', admin_event_passes_path(@event), class: 'btn btn-block btn-default' %>
  </div>
</div>